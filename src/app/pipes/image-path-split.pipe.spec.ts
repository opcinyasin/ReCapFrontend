import { ImagePathSplitPipe } from './image-path-split.pipe';

describe('ImagePathSplitPipe', () => {
  it('create an instance', () => {
    const pipe = new ImagePathSplitPipe();
    expect(pipe).toBeTruthy();
  });
});
